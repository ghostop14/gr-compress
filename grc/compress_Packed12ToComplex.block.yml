# auto-generated by grc.converter

id: compress_Packed12ToComplex
label: Packed12 To Complex
category: '[Compression]'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import compress
    make: compress.Packed12ToComplex()

documentation: |-
    This block takes 2 12-bit integers packed into 3 bytes and expands them to a complex.  This block reverses the ComplexToPacked12 process.

file_format: 1
