# auto-generated by grc.converter

id: compress_decompress
label: Decompress
category: '[Compression]'

parameters:
-   id: type
    label: Output Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
        typename: [complex, float, int, short, byte]
    hide: part
-   id: sendmsg
    label: Send Output Messages
    dtype: enum
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
-   id: verbose
    label: Verbose
    dtype: enum
    options: ['False', 'True']
    option_labels: ['No', 'Yes']

inputs:
-   domain: message
    id: compressedin

outputs:
-   domain: stream
    dtype: ${ type.typename }
-   domain: message
    id: uncompressed
    optional: true

templates:
    imports: import compress
    make: compress.decompress(${type.size},${sendmsg},${verbose})

file_format: 1
