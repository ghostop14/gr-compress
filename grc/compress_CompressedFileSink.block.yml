# auto-generated by grc.converter

id: compress_CompressedFileSink
label: Compressed File Sink
category: '[Compression]'

parameters:
-   id: file
    label: File
    dtype: file_save
-   id: unbuffered
    label: Unbuffered
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['Off', 'On']
-   id: append
    label: Append file
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Append, Overwrite]

inputs:
-   domain: message
    id: compressedin

templates:
    imports: import compress
    make: compress.CompressedFileSink(gr.sizeof_char, ${file}, ${unbuffered}, ${append})

documentation: "This compression file sink is designed to take a compressed message\
    \ from the compress block and write it to the specified file.\n\t\n\tNOTE: File\
    \ Format is chained sequences of blocks in the format: [Header][Compressed Block]\
    \ where [Header] is uint64_t uncompressed_size; uint64_t compressed_size.  Compressed\
    \ size defines the length of the [Compressed Block]\n\t\n\tThis block has a symetric\
    \ counterpart in the CompressedFileSource block to read it back in."

file_format: 1
